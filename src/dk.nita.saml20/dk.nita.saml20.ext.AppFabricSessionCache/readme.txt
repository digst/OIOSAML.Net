The app fabric cache must be started when using the app fabric session provider. That cache can be started by follwoing the steps below:

1. Open "Caching Administration Windows PowerShell" as administrator (elevated mode).
2. Run command Start-CacheCluster